A real-time face authentication attendance system built with deep learning and computer vision. Uses MTCNN for face detection and FaceNet for high-accuracy recognition, with an interactive Streamlit interface for user registration, punch-in/out, and automated attendance logging.

To run the project, install the required dependencies using pip install -r requirements.txt and launch the app with streamlit run app.py.
